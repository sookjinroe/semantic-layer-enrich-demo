# 다음 단계 — 시각화 데모 (이사 후 작업)

> 에이전트 설계 자체는 `render/`·`link/`의 설계문서·README에 정리돼 있다.
> 이 문서는 **다음 단계(시각화)의 방향과 열린 질문**만 담는다.

---

## 현재 상태 (1차 마무리)

- **Render** — 컬럼별 Description 생성, 소스 순차 탐색(DB→Catalog→Code), 충분성 자가 판단. 로직 동결. 하니스에 조회/실행 분리 UX 추가 완료(행 클릭=조회, ▷=실행, 전체 로그 토글, 소스 토글 시 결과 초기화).
- **Link** — Matcher(기존 Term 연결) → Discoverer(잔여에서 신호로 발굴) 파이프라인. 신호 기반 재설계 완료. BI 신호는 별도 `bi_assets`(N:N 참조, 역방향 조회)로 현실화. 증거 재요청 방지 가드 추가.
- 둘 다 실제 API로 동작, 검증 스크립트 5종 통과(`/home/claude/`), `/mnt/user-data/outputs/`의 `render/`·`link/`에 분리 보관.
- 지금 있는 건 **검증용 하니스** — 내가 엔진을 점검하는 도구.

---

## 다음 작업의 성격 — 디자인이 아니라 "투명성"

검증 하니스 ≠ 발표 데모. 하지만 데모의 목적을 **잘못 잡지 말 것:**

**❌ 아닌 것 (커머셜 디자인의 문법 — 하지 않는다):**
- 파이프라인 흐름도, 신호 종합 다이어그램 같은 설명용 그래픽
- 화면에서 특정 컬럼/결과를 하이라이트·강조하는 연출
- 메시지를 효과적으로 전달하려고 화면을 디자인하는 것
- → 이유: 화면이 뭔가를 강조·종합해 보여주면, 관객은 "저게 에이전트가 한 거냐, 시연자가 꾸민 거냐"를 의심한다. **가짜처럼 보여 시연의 신뢰가 깨진다.**

**⭕ 진짜 문제 (이걸 푼다):**
- 지금 화면은 **"어떤 신호로 어떤 추론을 거쳐 이 결과가 나왔는지"가 관객에게 안 보인다.**
- 우리는 대화·README로 맥락을 아니 로그를 읽으면 이해하지만, 처음 보는 관객은 "TAX_EXMP_FLG가 왜 충돌로 잔여가 됐지? 무슨 신호를 봤길래?"를 알 수 없다.
- 로그가 *판단 결과*는 보여주는데 *그 판단에 들어간 입력 신호*는 충분히 안 보인다. 지금 "보유: DB+CATALOG → 충분 → HIGH"로 **압축**돼 있어, 그 안에 뭐가 들었는지 안 보인다.

**→ 방향: 생략이 아니라 더 많이 보여주기.** 추론의 *재료*(입력 신호)를 드러낸다.
강조해서 *설득*하는 게 아니라, 다 보여줘서 관객이 *검증*하고 *납득*하게 한다.
**작업의 성격 = 투명성/노출. (디자인/강조 아님.)**

---

## 열린 질문 (이사 후 같이 따져볼 것 — 섣불리 정하지 않는다)

1. **입력 신호를 어디까지/어떻게 펼칠까.**
   - 입력(컬럼의 구조·Description·계보·참조 BI 자산, term_library)이 지금 코드 안에 숨어 있고 화면엔 결과만 나온다.
   - 관객이 "저 신호들로 이렇게 판단했구나"를 따라가려면 입력을 먼저/함께 봐야 한다.
   - 예: 컬럼을 보면 그 컬럼이 가진 모든 신호를 펼쳐 보이는 패널? — 방법·범위 미정.

2. **미리 준비해둔 데이터를 따로 보여주는 방법.**
   - 입력 신호(cluster.json, bi_assets, term_library)가 *무엇인지* 관객이 볼 수 있어야 한다.
   - 어떻게/어느 정도로 제시할지 미정.

3. **추론 과정의 노출 수준.**
   - 지금 압축된 로그를 어디까지 펼칠지. 모델이 실제로 본 신호 텍스트, 단계별 판단 근거를 관객이 검증 가능하게.

4. **BI 신호 전면화 (보류해둔 폴리싱 숙제).**
   - 좌측 칩을 뗀 상태. 사용 신호(라벨·지표·대시보드 공동참조)를 화면에 어떻게/얼마나 드러낼지.

---

## 강조할 점 — 화면이 아니라 "시연자가 짚는다"

화면에서 하이라이트하지 않는다. 시연자가 **말로** "이 컬럼을 보세요"라고 짚으면 충분하다.
(README의 케이스↔명제 매핑이 그 대본이 된다.) 화면은 그 컬럼의 신호·추론이 **정직하게 다 보이게**만 한다.

짚을 지점 (우선순위):

1. **"에이전트가 스스로 무엇이 부족한지 판단한다"** (Render)
   "소스가 많을수록 좋다"가 아니라, 충분한지 스스로 보고→부족하면 다음 소스→소진되면 정직하게 LOW. 결과 숫자가 아니라 *판단 경로*가 보일 때 "생각하고 있다"가 전달된다.

2. **"라벨이 없어도 관계로 개념을 발굴한다"** (Link · B2 담보) — **하이라이트**
   BI 라벨이 `dim_07`·`측정값11`(쓰레기)인데도, 공동참조·계보라는 *관계 신호*로 "이건 한 개념(담보)"이라고 잡아낸다. 라벨이 깨끗하면 사람도 안다 — 흩어지고 이름 없을 때 묶어내는 게 에이전트의 일. **데모의 핵심 가치.**

3. **"신호가 충돌하면 그 긴장이 판단 재료"** (Link · B3 신용점수)
   사용은 별개라는데 계보는 형제라는 충돌 → 억지 결론 대신 검토로. 모호함을 정직하게 다룬다.

4. **"발굴은 확정이 아니라 제안"** (Link · C 정직성)
   신규 개념은 조직이 합의하는 것. 신호 없으면(C1 고객분류) 지어내지 않고 사람에게.

**관통하는 한 문장:**
> 많은 신호로 확신을 과장하는 게 아니라, 신호를 정직하게 종합하고 모자라면 멈춘다.

---

## 가져갈 원칙 (놓치지 말 것)

- **로직·스키마 동결, 표현 계층만 손댄다.** 시각화하다 엔진을 건드리고 싶어지면 멈추고 의심.
- **render/ 와 link/ 절대 안 섞기.**
- **현실 왜곡 금지.** 보기 좋게 만들려고 데이터를 1:1로 누르거나 깨끗하게 꾸미면, 그게 에이전트 가치를 *숨기는* 왜곡이 된다 (B2 라벨이 그 교훈).
- **코드 능력 vs 데모 선택 구분.** A4처럼 — 코드 경로는 있되 이번 데모엔 안 보여주는 게 있을 수 있다.
- **하니스 데이터 수정 시:** 세 곳(cluster.json / term_library.json / 하니스 내장본) 동기화 + 검증 5종 재실행.

---

## 파일 위치

- `render/` — render_harness.jsx, README.md, schema.sql, catalog.json, code/
- `link/` — link_harness.jsx, README.md, cluster.json, term_library.json, naming_policy.json, link_agent_redesign.md
- 검증 스크립트 — `/home/claude/`의 validate_data / test_engine / test_harness_consistency / test_retrieval / test_parse (.mjs) *(컨테이너 리셋 시 재생성 필요)*
